# 忽略依赖与构建产物
node_modules/
**/node_modules/
dist/
**/dist/
.DS_Store
Thumbs.db
*.local
*.log
.cache/
.vite/
coverage/

# 非本仓库交付范围（本地可保留，不推送；避免 AI Studio 误启试用管理平台等）
admin/
mobile/

# 编辑器 / 本地
.idea/
.vscode/
*.swp
*.swo

# 环境变量（保留示例；本仓库演示账号见 README）
.env.local
.env.*.local

# 系统
desktop.ini

# 本地测试 / 截图产物（不推送）
.playwright-cli/
.pw-probe/
/*.png
mt-web/*.zip

# 本地工程副本（不推送，避免 AI Studio 识别混乱）
mt-web - 副本/
